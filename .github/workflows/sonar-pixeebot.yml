name: MULTIPLE FILES
on:
  check_run:
    types: [completed]

permissions:
  id-token: write

jobs:
  share:
    name: Testing URL
    runs-on: ubuntu-latest
    if: ${{ github.event.check_run.name == 'SonarCloud Code Analysis' }}
    steps:
      - uses: pixee/upload-tool-results-action@5f6e2ae2dd05bb8a4f2a24ae31e4f53444117639
        with:
          pixee-api-url: https://f4wf3v81x2.execute-api.us-east-1.amazonaws.com/prod
          tool: sonar
          sonar-token: ${{ secrets.SONAR_TOKEN }}
